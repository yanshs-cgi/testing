const App = () => (
  <pre>
{`Packages
framer-motion | For smooth status transitions and entry animations
lucide-react | For beautiful icons (already in base stack but good to confirm usage)

Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}`}
  </pre>
);

export default App;
